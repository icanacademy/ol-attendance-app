# Server Configuration
PORT=5001

# Database Configuration (uses same DB as scheduling app)
# IMPORTANT: Change these credentials for production!
DB_HOST=localhost
DB_PORT=5432
DB_NAME=scheduling_db
DB_USER=your_db_user
DB_PASSWORD=your_secure_password

# Admin Password for protected routes (tuition, commissions, holidays)
# IMPORTANT: Set a strong password for production!
ADMIN_PASSWORD=your_secure_admin_password

# Online Scheduler API URL
# This is where the attendance app fetches student/teacher data from
SCHEDULER_API_URL=http://localhost:4488/api

# CORS - Allowed Origins (comma-separated list of allowed frontend URLs)
# For production, set this to your actual frontend domain(s)
# Example: https://attendance.yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:5174,http://localhost:5173

# Node Environment
NODE_ENV=development
